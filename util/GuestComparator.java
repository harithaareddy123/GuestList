package util;

import java.util.Comparator;

import entity.GuestDetails;

public class GuestComparator implements Comparator<GuestDetails>
	{

	public int compare(GuestDetails o1, GuestDetails o2) {
		
if(o1.getId()>o1.getId())
{
		return 1;
	}
if(o1.getId()<o1.getId())
{
		return -1;
	}
return 0;

	}
	}

